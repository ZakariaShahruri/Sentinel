apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: uptime-kuma
spec:
  to:
    kind: Service
    name: uptime-kuma
  port:
    targetPort: 3001
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
